package uasz.sn.microservice_repartition.model;
/*
import org.springframework.cloud.openfeign.FeignClient;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import uasz.sn.microservice_repartition.DTO.EnseignementDTO;

import java.util.List;

@FeignClient(name="service-maquette", url = "http://localhost:8086")
public interface EnseignementClient {
    @GetMapping("/enseignements/{id}")
    EnseignementDTO getEnseignement(@PathVariable Long id);

    @GetMapping("/enseignements/esclaves")
    List<EnseignementDTO> getAllEnseignements();
    @GetMapping("/service-maquette/actuator/health")
    ResponseEntity<String> isAvailable();

}*/